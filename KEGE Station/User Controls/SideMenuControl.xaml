<UserControl x:Class="KEGE_Station.User_Controls.SideMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="600">
    <UserControl.Resources>

        <ControlTemplate x:Key="NoMouseOverButtonTemplate" TargetType="Button">
            <Border x:Name="InnerBorder" Background="#ffffff" BorderBrush="#000000" BorderThickness="3" CornerRadius="15">
                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="{x:Static SystemColors.ControlLightBrush}" />
                    <Setter Property="Foreground" Value="{x:Static SystemColors.GrayTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

    </UserControl.Resources>
    <Border Background="#2D6DFE">
        <Grid>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,15,15,0">
                <Button x:Name="Home_btn" Width="70" Height="70" Margin="0,0,10,0" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="Home_btn_Click">
                    <Image Source="/Resources/Icons/home96x96.png" Margin="10 5 10 10"/>
                </Button>
                <Button x:Name="Settings_btn" Width="70" Height="70" Margin="0,0,10,0" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="Settings_btn_Click">
                    <Image Source="/Resources/Icons/settings96x96.png" Margin="10"/>
                </Button>
                <Button x:Name="Exit_btn" Width="70" Height="70" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="Exit_btn_Click">
                    <Image Source="/Resources/Icons/buttonClose96x96.png" Margin="10"/>
                </Button>
            </StackPanel>

            <StackPanel Margin="0 70 0 0" Width="460" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Button x:Name="OpenGenerator_btn" Content="Генератор" Margin="0, 0, 0, 30" 
                        Width="460" Height="111" FontSize="32" FontWeight="SemiBold" 
                        FontFamily="/Resources/Fonts/#Inter" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="OpenGenerator_btn_Click"/>

                <Button x:Name="OpenEdit_btn" Content="Редактировать вариант" Margin="0, 0, 0, 30" 
                        Width="460" Height="111" FontSize="32" FontWeight="SemiBold" 
                        FontFamily="/Resources/Fonts/#Inter" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="OpenEdit_btn_Click"/>

                <Button x:Name="OpenResults_btn" Content="Проверить результаты" 
                        Width="460" Height="111" FontSize="32" FontWeight="SemiBold" 
                        FontFamily="/Resources/Fonts/#Inter" 
                        Template="{StaticResource NoMouseOverButtonTemplate}" Click="OpenResults_btn_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>